# ⚙️ AVTO-POLIS — Backend (NestJS)

Ця директорія містить серверну частину платформи **Avto-Polis**, побудовану на базі **NestJS**.

## ✨ Особливості
*   **Modular Architecture**: Чіткий поділ на модулі (Auth, News, Consultations, Prisma).
*   **Prisma ORM**: Сучасна робота з базою даних PostgreSQL.
*   **JWT Authentication**: Захист адмін-панелі за допомогою JSON Web Tokens.
*   **Data Validation**: Строга перевірка вхідних даних за допомогою DTO та `class-validator`.
*   **Swagger/OpenAPI**: Автоматична документація API.

## 🛠 Технології
*   **Framework**: [NestJS](https://nestjs.com/)
*   **ORM**: [Prisma](https://www.prisma.io/)
*   **Security**: Passport.js, JWT, Bcrypt
*   **Database**: PostgreSQL (через Supabase або локально)

## 🚀 Запуск локально
1. Встановіть залежності:
   ```bash
   npm install
   ```
2. Налаштуйте змінні оточення у файлі `.env`:
   ```env
   DATABASE_URL="postgresql://user:password@localhost:5432/avto_polis"
   JWT_SECRET="your_secret_key"
   PORT=4000
   ```
3. Згенеруйте клієнт Prisma та синхронізуйте БД:
   ```bash
   npx prisma generate
   npx prisma db push
   ```
4. Запустіть сервер:
   ```bash
   npm run start:dev
   ```

API буде доступне за адресою [http://localhost:4000](http://localhost:4000).
Документація Swagger: [http://localhost:4000/api](http://localhost:4000/api) (якщо активовано).

## 📁 Структура папок
*   `/src/auth`: Логіка авторизації та захисту маршрутів.
*   `/src/news`: Модуль керування новинами.
*   `/src/consultations`: Модуль обробки заявок на консультацію.
*   `/src/prisma`: Глобальний сервіс для роботи з БД.
*   `/prisma`: Схема бази даних (`schema.prisma`).

---
Для отримання повної інформації про проєкт зверніться до [головного README](../README.md).
